{
  "name": "gonglijeongli-web",
  "private": true,
  "packageManager": "npm@11.6.2",
  "workspaces": [
    "front",
    "back"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "fix": "turbo run fix",
    "clean": "turbo run clean",
    "reset": "turbo run reset"
  },
  "devDependencies": {
    "turbo": "^2.8.14"
  }
}
